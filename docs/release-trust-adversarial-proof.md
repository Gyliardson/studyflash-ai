# Release trust adversarial proof fixture

Disposable proof-only marker for issue #69. This branch/PR must never be merged. It exists to exercise trusted promotion lifecycle, retargeting, stale-SHA, and wrong-base behavior against the live GitHub control plane.
