generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"] // Necessário para o Vercel Serverless
}

datasource db {
  provider = "postgresql"
}

// --- SEUS MODELOS ---

model Flashcard {
  id        String   @id @default(uuid())
  userId    String
  frente    String
  verso     String
  createdAt DateTime @default(now())
  deckId    String?
  deck      Deck?    @relation(fields: [deckId], references: [id])

  @@index([userId])
}

model Deck {
  id        String      @id @default(uuid())
  userId    String
  nome      String
  createdAt DateTime    @default(now())
  cards     Flashcard[]

  @@index([userId])
}